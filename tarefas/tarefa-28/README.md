# Tarefa 28

A equipe de suporte interno revisou uma base de conhecimento.

## O que fazer

1. Crie o arquivo `resposta-padrao.txt` com o texto `Resposta padrão criada.`.
2. Altere o status em `base.txt` para `concluído`.
3. Corrija o valor de `revisao` no arquivo `artigo.json` para `2`.

## Arquivos envolvidos

- `resposta-padrao.txt`
- `base.txt`
- `artigo.json`

## Para passar na validação automática

- O arquivo `resposta-padrao.txt` deve existir e conter exatamente: `Resposta padrão criada.`
- O arquivo `base.txt` deve conter exatamente: `Status: concluído`
- No arquivo `artigo.json`, o campo `revisao` deve ter o valor `2`.
- Nenhum arquivo fora de `tarefas/tarefa-28` deve ser alterado.

A validação será feita automaticamente quando o Pull Request for aberto no GitHub.

## Lembrete

Mexa apenas nos arquivos desta tarefa. As outras pastas pertencem a outros colegas.
