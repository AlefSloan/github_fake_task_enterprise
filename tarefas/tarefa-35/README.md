# Tarefa 35

A equipe de gestão quer fechar a última tarefa de revisão interna.

## O que fazer

1. Corrija o valor de `metaMensal` no arquivo `meta.json` para `35`.
2. Corrija o resultado da conta no arquivo `resultado.txt`.
3. Corrija o nome do cliente no arquivo `cliente.txt`.

## Arquivos envolvidos

- `meta.json`
- `resultado.txt`
- `cliente.txt`

## Para passar na validação automática

- No arquivo `meta.json`, o campo `metaMensal` deve ter o valor `35`.
- O arquivo `resultado.txt` deve conter exatamente: `25 + 10 = 35`
- O arquivo `cliente.txt` deve conter exatamente: `Cliente: Final Tech`
- Nenhum arquivo fora de `tarefas/tarefa-35` deve ser alterado.

A validação será feita automaticamente quando o Pull Request for aberto no GitHub.

## Lembrete

Mexa apenas nos arquivos desta tarefa. As outras pastas pertencem a outros colegas.
