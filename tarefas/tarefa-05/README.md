# Tarefa 05

O financeiro separou pequenas correções para fechar uma conferência.

## O que fazer

1. Corrija o resultado da conta no arquivo `soma.txt`.
2. Corrija o valor de `parcelas` no arquivo `fatura.json` para `3`.
3. Corrija o nome do cliente no arquivo `cliente.txt`.

## Arquivos envolvidos

- `soma.txt`
- `fatura.json`
- `cliente.txt`

## Para passar na validação automática

- O arquivo `soma.txt` deve conter exatamente: `30 + 15 = 45`
- No arquivo `fatura.json`, o campo `parcelas` deve ter o valor `3`.
- O arquivo `cliente.txt` deve conter exatamente: `Cliente: Loja Solare`
- Nenhum arquivo fora de `tarefas/tarefa-05` deve ser alterado.

A validação será feita automaticamente quando o Pull Request for aberto no GitHub.

## Lembrete

Mexa apenas nos arquivos desta tarefa. As outras pastas pertencem a outros colegas.
