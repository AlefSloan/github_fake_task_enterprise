# Tarefa 29

O administrativo recebeu uma correção de documentos de fornecedor.

## O que fazer

1. Corrija o nome do fornecedor no arquivo `fornecedor.txt`.
2. Mova `entrada/contrato.txt` para `contratos/contrato.txt`.
3. Renomeie `cadastro_fornecedro.txt` para `cadastro_fornecedor.txt`.

## Arquivos envolvidos

- `fornecedor.txt`
- `entrada/contrato.txt`
- `contratos/contrato.txt`
- `cadastro_fornecedro.txt`
- `cadastro_fornecedor.txt`

## Para passar na validação automática

- O arquivo `fornecedor.txt` deve conter exatamente: `Fornecedor: Papelaria Estrela`
- O arquivo `contratos/contrato.txt` deve existir e conter exatamente: `Contrato de fornecedor.`
- O arquivo `entrada/contrato.txt` não deve mais existir neste local.
- O arquivo `cadastro_fornecedor.txt` deve existir e conter exatamente: `Cadastro de fornecedor.`
- O arquivo `cadastro_fornecedro.txt` não deve mais existir neste local.
- Nenhum arquivo fora de `tarefas/tarefa-29` deve ser alterado.

A validação será feita automaticamente quando o Pull Request for aberto no GitHub.

## Lembrete

Mexa apenas nos arquivos desta tarefa. As outras pastas pertencem a outros colegas.
