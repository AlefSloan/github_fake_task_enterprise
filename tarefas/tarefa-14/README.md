# Tarefa 14

O time de dados preparou uma limpeza simples em arquivos de cliente.

## O que fazer

1. Corrija o valor de `id` no arquivo `cliente.json` para `141`.
2. Corrija o nome do cliente no arquivo `cliente.txt`.
3. Corrija o título principal em `registro.md` para usar apenas um `#`.

## Arquivos envolvidos

- `cliente.json`
- `cliente.txt`
- `registro.md`

## Para passar na validação automática

- No arquivo `cliente.json`, o campo `id` deve ter o valor `141`.
- O arquivo `cliente.txt` deve conter exatamente: `Cliente: Norte Tech`
- O arquivo `registro.md` deve conter exatamente: `# Registro do cliente`
- Nenhum arquivo fora de `tarefas/tarefa-14` deve ser alterado.

A validação será feita automaticamente quando o Pull Request for aberto no GitHub.

## Lembrete

Mexa apenas nos arquivos desta tarefa. As outras pastas pertencem a outros colegas.
