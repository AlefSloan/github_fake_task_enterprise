# Tarefa 20

A equipe de qualidade apontou ajustes em uma verificação simples.

## O que fazer

1. Corrija o título principal em `qualidade.md` para usar apenas um `#`.
2. Altere o status em `verificacao.txt` para `concluído`.
3. Corrija o valor de `nota` no arquivo `qualidade.json` para `8`.

## Arquivos envolvidos

- `qualidade.md`
- `verificacao.txt`
- `qualidade.json`

## Para passar na validação automática

- O arquivo `qualidade.md` deve conter exatamente: `# Relatório de qualidade`
- O arquivo `verificacao.txt` deve conter exatamente: `Status: concluído`
- No arquivo `qualidade.json`, o campo `nota` deve ter o valor `8`.
- Nenhum arquivo fora de `tarefas/tarefa-20` deve ser alterado.

A validação será feita automaticamente quando o Pull Request for aberto no GitHub.

## Lembrete

Mexa apenas nos arquivos desta tarefa. As outras pastas pertencem a outros colegas.
