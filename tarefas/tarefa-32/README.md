# Tarefa 32

A área técnica organizou documentos de ambiente sem exigir programação.

## O que fazer

1. Renomeie `ambiente_producaoo.txt` para `ambiente_producao.txt`.
2. Crie o arquivo `responsavel.txt` com o texto `Responsável definido.`.
3. Corrija o valor de `portas` no arquivo `ambiente.json` para `3`.

## Arquivos envolvidos

- `ambiente_producaoo.txt`
- `ambiente_producao.txt`
- `responsavel.txt`
- `ambiente.json`

## Para passar na validação automática

- O arquivo `ambiente_producao.txt` deve existir e conter exatamente: `Ambiente de produção.`
- O arquivo `ambiente_producaoo.txt` não deve mais existir neste local.
- O arquivo `responsavel.txt` deve existir e conter exatamente: `Responsável definido.`
- No arquivo `ambiente.json`, o campo `portas` deve ter o valor `3`.
- Nenhum arquivo fora de `tarefas/tarefa-32` deve ser alterado.

A validação será feita automaticamente quando o Pull Request for aberto no GitHub.

## Lembrete

Mexa apenas nos arquivos desta tarefa. As outras pastas pertencem a outros colegas.
